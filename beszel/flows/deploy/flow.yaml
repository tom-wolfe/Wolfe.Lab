id: deploy
namespace: lab.beszel
description: |
  Deploys the Beszel compose stack on a green Chezmoi tick.
  Note: the agent is a host process installed separately from the Brewfile.

labels:
  job: deploy
  alert: low

concurrency:
  limit: 1
  behavior: QUEUE

tasks:
  - id: converge
    type: io.kestra.plugin.scripts.shell.Commands
    timeout: PT20M
    commands:
      - /Users/tomwolfe/.local/share/chezmoi/scripts/deploy.sh beszel

triggers:
  - id: after-tick
    type: io.kestra.plugin.core.trigger.Flow
    preconditions:
      id: chezmoi-tick-green
      flows:
        - namespace: lab.chezmoi
          flowId: update
          states: [SUCCESS]
