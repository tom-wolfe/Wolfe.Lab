name: tofu chezmoi

on:
  push:
    branches: [main]
    paths: [chezmoi/tofu/**, scripts/apply.sh, scripts/plan.sh, .forgejo/workflows/tofu-chezmoi.yaml]
  schedule:
    - cron: "10 7 * * *"
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  apply:
    if: github.event_name == 'push'
    runs-on: MacMini
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: scripts/apply.sh chezmoi

      - if: failure()
        run: |
          scripts/alert.sh "tofu apply chezmoi failed"

  plan:
    if: github.event_name != 'push'
    runs-on: MacMini
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - id: plan
        run: |
          set +e
          scripts/plan.sh chezmoi
          rc=$?
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          exit $rc

      - if: failure()
        run: |
          scripts/alert.sh "tofu chezmoi: ${{ steps.plan.outputs.rc == '2' && 'drift or pending change' || 'plan failed' }}"
