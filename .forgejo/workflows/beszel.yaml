name: beszel

on:
  push:
    branches: [main]
    paths: [beszel/**, scripts/deploy.sh, .forgejo/workflows/beszel.yaml]
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: MacMini
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - run: scripts/deploy.sh beszel

      - if: failure()
        run: |
          scripts/alert.sh "deploy beszel failed on MacMini" \
            "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_number }}"
