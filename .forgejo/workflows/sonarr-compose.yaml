name: sonarr compose

on:
  pull_request:
  push:
    branches: [main]
    paths: [sonarr/compose/**, .forgejo/workflows/sonarr-compose.yaml]
  workflow_dispatch:

jobs:
  check:
    # Pre-merge code never runs on a node.
    if: github.event_name == 'pull_request'
    runs-on: docker
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - working-directory: sonarr/compose
        run: lab check

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: MacMini
    timeout-minutes: 20
    concurrency:
      group: MacMini
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
      - working-directory: sonarr/compose
        run: lab deploy --auto-approve
