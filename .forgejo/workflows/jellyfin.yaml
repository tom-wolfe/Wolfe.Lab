name: jellyfin

on:
  push:
    branches: [main]
    paths: [jellyfin/**, scripts/deploy.sh, .forgejo/workflows/jellyfin.yaml]
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: MacMini
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - run: scripts/deploy.sh jellyfin /Volumes/Data1 /Volumes/Data2

      - if: failure()
        run: |
          scripts/alert.sh "deploy jellyfin failed on MacMini" \
            "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_number }}"
