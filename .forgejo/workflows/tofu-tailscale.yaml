name: tofu tailscale

on:
  push:
    branches: [main]
    paths: [tailscale/tofu/**, scripts/apply.sh, scripts/plan.sh, .forgejo/workflows/tofu-tailscale.yaml]
  schedule:
    - cron: "40 7 * * *"
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  apply:
    if: github.event_name == 'push'
    runs-on: MacMini
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: scripts/apply.sh tailscale

      - if: failure()
        run: |
          scripts/alert.sh "tofu apply tailscale failed"

  plan:
    if: github.event_name != 'push'
    runs-on: MacMini
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - id: plan
        run: |
          set +e
          scripts/plan.sh tailscale
          rc=$?
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          exit $rc

      - if: failure()
        run: |
          scripts/alert.sh "tofu tailscale: ${{ steps.plan.outputs.rc == '2' && 'drift or pending change' || 'plan failed' }}"
