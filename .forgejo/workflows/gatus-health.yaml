name: gatus health

on:
  schedule:
    - cron: "11-59/15 * * * *"
  workflow_dispatch:

jobs:
  probe:
    runs-on: MacMini
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v7
      - working-directory: gatus/health
        run: lab probe
