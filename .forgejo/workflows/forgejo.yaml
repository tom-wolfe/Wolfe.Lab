name: forgejo

on:
  push:
    branches: [main]
    paths: [forgejo/**, .forgejo/workflows/forgejo.yaml]
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: MacMini
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - working-directory: forgejo
        run: dotnet run --project ../build/src/Wolfe.Lab.Build -- deploy --auto-approve
