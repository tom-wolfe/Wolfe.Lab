name: qbittorrent

on:
  push:
    branches: [main]
    paths: [qbittorrent/**, scripts/deploy.sh, .forgejo/workflows/qbittorrent.yaml]
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: MacMini
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - run: scripts/deploy.sh qbittorrent /Volumes/Data1 /Volumes/Data2

      - if: failure()
        run: |
          scripts/alert.sh "deploy qbittorrent failed on MacMini"
