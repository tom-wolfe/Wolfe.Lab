name: radarr backup

on:
  schedule:
    - cron: "45 2 * * *"
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  backup:
    runs-on: MacMini
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
      - working-directory: radarr/backup
        run: lab backup

  # The snapshot just taken, restored to scratch and asserted.
  restore-drill:
    needs: backup
    runs-on: MacMini
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - working-directory: radarr/backup
        run: lab restore-drill
