name: mail

on:
  push:
    branches: [main]
    paths: [mail/**, .forgejo/workflows/mail.yaml]
  workflow_dispatch:

concurrency:
  group: MacMini
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: MacMini
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - working-directory: mail
        run: dotnet run --project ../build/src/Wolfe.Lab.Build -- deploy --auto-approve
