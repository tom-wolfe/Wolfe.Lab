name: heartbeat tofu

on:
  pull_request:
  push:
    branches: [main]
    paths: [heartbeat/tofu/**, .forgejo/workflows/heartbeat-tofu.yaml]
  workflow_dispatch:

jobs:
  check:
    if: github.event_name == 'pull_request'
    runs-on: MacMini
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - working-directory: heartbeat/tofu
        run: lab check

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: MacMini
    timeout-minutes: 30
    concurrency:
      group: MacMini
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
      - working-directory: heartbeat/tofu
        run: lab deploy --auto-approve
