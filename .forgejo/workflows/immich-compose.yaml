name: immich compose

on:
  pull_request:
  push:
    branches: [main]
    paths: [immich/compose/**, .forgejo/workflows/immich-compose.yaml]
  workflow_dispatch:

jobs:
  check:
    # Pre-merge code never runs on a node.
    if: github.event_name == 'pull_request'
    runs-on: docker
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - working-directory: immich/compose
        run: lab check

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: MacMini
    timeout-minutes: 30
    concurrency:
      group: MacMini
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - working-directory: immich/compose
        run: lab deploy --auto-approve
