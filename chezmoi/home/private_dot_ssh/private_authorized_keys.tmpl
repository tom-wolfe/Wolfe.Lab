ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIH5fg4Y79OJ/zq4eIxkY5g5lPIEimcT1veW2ERgZYslZ tom-1password
{{ if and .server (eq .chezmoi.os "darwin") -}}
# Linux nodes back up into the restic repository on Data2 over SFTP
# (restic/README.md "From a Linux node"). Each node's key can only run
# sftp-server here: restrict = no forwarding, no pty, no agent.
{{ range list "wolfe-pi5" -}}
restrict,command="/usr/libexec/sftp-server" {{ onepasswordRead (printf "op://Wolfe.Lab/restic-sftp-%s/public key" .) }} restic-sftp-{{ . }}
{{ end -}}
{{ end -}}
