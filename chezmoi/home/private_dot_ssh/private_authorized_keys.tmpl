ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIH5fg4Y79OJ/zq4eIxkY5g5lPIEimcT1veW2ERgZYslZ tom-1password
{{- if eq .machine "server" }}
{{- /* The Kestra job-bridge key (see kestra/README.md): generated on
       this machine by the bootstrap script, so it appears only after the
       first apply has run. `restrict` kills pty/forwarding/agent; `command`
       makes sshd run the lab-job dispatcher instead of the client's request. */}}
{{- $pub := joinPath .chezmoi.homeDir "Docker/kestra/ssh/id_ed25519.pub" }}
{{- if stat $pub }}
restrict,command="{{ .chezmoi.homeDir }}/.local/bin/lab-job" {{ include $pub | trim }}
{{- end }}
{{- end }}
