# Materialized ONCE from 1Password by chezmoi — create_ semantics, see
# Docker/garage/ for the doctrine. Same vault item as KESTRA_DB_PASSWORD
# in kestra.env: the two files MUST agree, and sourcing both from the one
# item is what guarantees they do.
POSTGRES_PASSWORD={{ onepasswordRead "op://Wolfe.Lab/kestra-postgres/password" }}
