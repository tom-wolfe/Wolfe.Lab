# Materialized ONCE from 1Password by chezmoi — the vault is the origin,
# this file is a cache. `create_` semantics: written only when MISSING,
# template never evaluated otherwise, so `op` (and the internet) are a
# bootstrap dependency, not a tick dependency. A failed read aborts the
# apply with nothing written — no empty husk can satisfy the exists-check.
# Rotation: delete this file and `chezmoi apply`.
# garage/compose.yaml reads it via env_file.
GARAGE_RPC_SECRET={{ onepasswordRead "op://Wolfe.Lab/garage-rpc-secret/credential" }}
GARAGE_ADMIN_TOKEN={{ onepasswordRead "op://Wolfe.Lab/garage-s3-admin-token/credential" }}
