# Materialized ONCE from 1Password by chezmoi — the vault is the origin,
# this file is a cache. `create_` semantics: chezmoi writes it only when
# it's MISSING and never evaluates the template otherwise, so `op` (and
# the internet) are a bootstrap dependency, not a tick dependency.
# Rotation: delete this file and `chezmoi apply`.
# The key is NordVPN's WireGuard (NordLynx) private key — from the Nord
# dashboard's Manual configuration section, NOT the account password.
# See qbittorrent/README.md.
WIREGUARD_PRIVATE_KEY={{ onepasswordRead "op://Wolfe.Lab/nordvpn-wireguard/credential" }}
