id: apply
namespace: lab.chezmoi
description: Manual. Deploys the Chezmoi Tofu stack.

labels:
  job: apply
  alert: low

concurrency:
  limit: 1
  behavior: QUEUE

tasks:
  - id: apply
    type: io.kestra.plugin.fs.ssh.Command
    timeout: PT30M
    commands:
      - chezmoi/apply
