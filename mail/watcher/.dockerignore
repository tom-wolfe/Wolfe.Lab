# The image is built from this directory, which is also the component: the
# compose file, its secrets and the build outputs are not the application.
compose.yaml
secrets.env
ritten.json
**/bin
**/obj
artifacts
temp
TestResults
